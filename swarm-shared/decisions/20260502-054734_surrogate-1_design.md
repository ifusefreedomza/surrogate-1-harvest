# surrogate-1 / design

### Diagnosis
* The project lacks a robust implementation for handling Hugging Face API rate limits, which can block dataset training.
* There is inadequate reuse of existing Lightning Studio instances, leading to wasted quota and potential downtime.
* The data ingestion pipeline is not optimized, resulting in inefficient use of resources and potential data inconsistencies.
* The project does not have a clear strategy for handling dataset schema inconsistencies, which can lead to errors during training.
* The current implementation does not take advantage of the Hugging Face CDN bypass, which can significantly reduce the load on the API and improve training efficiency.

### Proposed change
The proposed change will focus on implementing a robust solution for handling Hugging Face API rate limits and optimizing the data ingestion pipeline. Specifically, the change will be made to the `train.py` file, which is responsible for training the model using the Hugging Face dataset.

### Implementation
To implement the proposed change, the following steps will be taken:
1. Modify the `train.py` file to use the Hugging Face CDN bypass for downloading dataset files, reducing the load on the API and improving training efficiency.
2. Implement a retry mechanism with a 360-second wait period to handle Hugging Face API rate limit errors (429 status code).
3. Optimize the data ingestion pipeline by downloading each file individually via `hf_hub_download` and projecting to `{prompt, response}` only at parse time.
4. Ensure that the `train.py` file is executable and has the proper Bash shebang (`#!/usr/bin/env bash`).

Example code snippet:
```python
import os
import requests
from datasets import load_dataset
from hf_hub_download import download

# Define the Hugging Face dataset repository and file path
repo_id = "username/repo_name"
file_path = "path/to/file"

# Download the dataset file using the Hugging Face CDN bypass
def download_file(repo_id, file_path):
    url = f"https://huggingface.co/datasets/{repo_id}/resolve/main/{file_path}"
    response = requests.get(url)
    with open(file_path, "wb") as f:
        f.write(response.content)

# Load the dataset using the downloaded file
def load_dataset(file_path):
    dataset = load_dataset("json", data_files=[file_path])
    return dataset

# Train the model using the loaded dataset
def train_model(dataset):
    # Train the model using the dataset
    pass

# Main function
def main():
    # Download the dataset file
    download_file(repo_id, file_path)

    # Load the dataset
    dataset = load_dataset(file_path)

    # Train the model
    train_model(dataset)

if __name__ == "__main__":
    main()
```
To handle Hugging Face API rate limit errors, the following retry mechanism can be implemented:
```python
import time

def retry_func(func, max_retries=5, retry_delay=360):
    for attempt in range(max_retries):
        try:
            return func()
        except requests.exceptions.HTTPError as e:
            if e.response.status_code == 429:
                print(f"Rate limit error, retrying in {retry_delay} seconds...")
                time.sleep(retry_delay)
            else:
                raise
    raise Exception("Max retries exceeded")
```
### Verification
To verify that the proposed change works, the following steps can be taken:
1. Run the `train.py` file and monitor the API requests to ensure that the Hugging Face CDN bypass is being used correctly.
2. Simulate a rate limit error by making multiple requests to the Hugging Face API in a short period.
3. Verify that the retry mechanism is working correctly by checking the logs and ensuring that the training process resumes after the retry delay.
4. Monitor the training process and verify that the model is being trained correctly using the loaded dataset.
