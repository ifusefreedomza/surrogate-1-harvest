# axiomops / ops

### Highest-Value Incremental Improvement
Based on the provided information, the highest-value incremental improvement that can be shipped in under 2 hours is to implement a fix for the HF API rate limit issue. This involves pre-listing file paths once and embedding them in the training script to bypass the API rate limit.

### Implementation Plan
1. **Identify the relevant script**: Locate the training script (`train.py`) that is currently being used.
2. **Modify the script**: Update the script to use the CDN bypass method for downloading dataset files.
3. **Pre-list file paths**: Make a single API call to `list_repo_tree(path, recursive=False)` for one date folder and save the list to a JSON file.
4. **Embed file list in script**: Modify the training script to read the file list from the JSON file and use it to download the dataset files from the CDN.

### Code Snippets
```python
import json
import requests

# Pre-list file paths
def get_file_list(repo, path):
    response = requests.get(f"https://huggingface.co/{repo}/resolve/main/{path}")
    file_list = response.json()
    with open("file_list.json", "w") as f:
        json.dump(file_list, f)

# Embed file list in script
def load_dataset(file_list):
    dataset = []
    for file in file_list:
        response = requests.get(f"https://huggingface.co/{repo}/resolve/main/{file}")
        dataset.append(response.json())
    return dataset

# Example usage
repo = "your-repo"
path = "your-path"
get_file_list(repo, path)
with open("file_list.json", "r") as f:
    file_list = json.load(f)
dataset = load_dataset(file_list)
```
### Deployment
1. **Update the script**: Commit the changes to the `train.py` script.
2. **Run the script**: Execute the updated script to test the changes.
3. **Monitor the results**: Verify that the API rate limit issue is resolved and the dataset files are being downloaded successfully from the CDN.
