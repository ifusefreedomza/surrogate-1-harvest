# surrogate-1 / design

### Diagnosis
* The project lacks a robust implementation for handling Hugging Face API rate limits on the discovery side, which can block dataset training.
* The existing implementation may not be reusing existing Lightning Studio instances efficiently, leading to wasted resources.
* The project does not have a mechanism to bypass the Hugging Face API rate limit for dataset training, which can cause significant delays.
* The implementation of the `knowledge-rag` pipeline is not integrated with the business research script, which can lead to missed insights.
* The project does not have a clear strategy for handling the Lightning H200 GPU instance, which can lead to inefficient resource utilization.

### Proposed change
The proposed change is to implement a robust mechanism to bypass the Hugging Face API rate limit for dataset training and to reuse existing Lightning Studio instances efficiently. This can be achieved by modifying the `granite-business-research.sh` script to use the HF CDN bypass and by implementing a studio reuse mechanism in the `train.py` script.

### Implementation
To implement the proposed change, the following steps can be taken:
1. Modify the `granite-business-research.sh` script to use the HF CDN bypass by replacing the `list_repo_files` API call with a direct download from the HF CDN.
2. Implement a studio reuse mechanism in the `train.py` script by listing existing studios and reusing the running ones.
3. Update the `train.py` script to use the HF CDN bypass for dataset training.

Example code snippet:
```python
import os
import json
import requests

# List existing studios and reuse the running ones
def reuse_studio():
    studios = Teamspace.studios
    for studio in studios:
        if studio.name == "surrogate-1" and studio.status == "Running":
            return studio
    return None

# Download dataset files from HF CDN
def download_dataset_files(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    return response.content

# Update train.py script to use HF CDN bypass
def train():
    studio = reuse_studio()
    if studio is None:
        studio = Teamspace.create_studio("surrogate-1")
    dataset_files = download_dataset_files("axentx/surrogate-1", "data/")
    # Train model using dataset files
```
### Verification
To verify that the proposed change works, the following steps can be taken:
1. Run the modified `granite-business-research.sh` script and verify that it uses the HF CDN bypass correctly.
2. Run the modified `train.py` script and verify that it reuses existing Lightning Studio instances efficiently and uses the HF CDN bypass for dataset training.
3. Monitor the project's resource utilization and verify that it has improved significantly.
4. Verify that the project can handle the Lightning H200 GPU instance efficiently and that the `knowledge-rag` pipeline is integrated with the business research script correctly.
