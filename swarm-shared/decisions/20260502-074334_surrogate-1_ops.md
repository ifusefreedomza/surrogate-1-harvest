# surrogate-1 / ops

### Diagnosis
* The project lacks a robust implementation for handling Hugging Face API rate limits on both discovery and frontend sides, which can block dataset training.
* The existing implementation may not be reusing existing Lightning Studio instances efficiently, leading to wasted resources and quota.
* The project does not have a mechanism to bypass the Hugging Face API rate limit entirely, which can be achieved by downloading public dataset files directly from the CDN.
* The training pipeline may be interrupted by Lightning idle stop, which can kill the training process.
* The project does not have a robust implementation for handling Kaggle KGAT token authentication, which can return 401 errors.

### Proposed change
The proposed change is to implement a mechanism to bypass the Hugging Face API rate limit by downloading public dataset files directly from the CDN, and to reuse existing Lightning Studio instances efficiently. This can be achieved by modifying the `train.py` script to download dataset files from the CDN and to reuse existing Lightning Studio instances.

### Implementation
To implement this change, we can modify the `train.py` script as follows:
```python
import json
import requests

# Download dataset files from CDN
def download_dataset_files(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    return response.json()

# Reuse existing Lightning Studio instances
def reuse_lightning_studio(studio_name):
    for s in Teamspace.studios:
        if s.name == studio_name and s.status == 'Running':
            return s
    return None

# Modify train.py to download dataset files from CDN and reuse existing Lightning Studio instances
def train():
    # Download dataset files from CDN
    dataset_files = download_dataset_files("dataset_repo", "path/to/dataset")

    # Reuse existing Lightning Studio instances
    studio = reuse_lightning_studio("studio_name")
    if studio is None:
        # Create a new Lightning Studio instance
        studio = Teamspace.create_studio("studio_name")

    # Train the model
    # ...

# Save the list of dataset files to a JSON file
def save_dataset_files(repo, path):
    dataset_files = download_dataset_files(repo, path)
    with open("dataset_files.json", "w") as f:
        json.dump(dataset_files, f)

# Load the list of dataset files from a JSON file
def load_dataset_files():
    with open("dataset_files.json", "r") as f:
        return json.load(f)

# Modify the train.py script to load the list of dataset files from a JSON file
def train():
    # Load the list of dataset files from a JSON file
    dataset_files = load_dataset_files()

    # ...
```
We can also add a mechanism to handle Lightning idle stop by checking the status of the Lightning Studio instance before each training iteration and restarting it if it is stopped.

### Verification
To verify that the proposed change works, we can check the following:

* The dataset files are downloaded successfully from the CDN.
* The existing Lightning Studio instances are reused efficiently.
* The training pipeline is not interrupted by Lightning idle stop.
* The Kaggle KGAT token authentication works correctly.

We can verify these by checking the logs and the output of the `train.py` script, and by monitoring the Lightning Studio instances and the training pipeline.
