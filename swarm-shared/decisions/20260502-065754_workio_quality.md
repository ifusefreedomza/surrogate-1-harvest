# workio / quality

### High-Value Incremental Improvement for Workio Quality
#### Task: Implement HF CDN Bypass for Dataset Training
#### Time Estimate: < 2 hours

### Implementation Plan
#### Step 1: Update `train.py` to Use HF CDN for Dataset Download
Modify the `train.py` script to download datasets from the HF CDN instead of using the HF API. This can be achieved by replacing the `load_dataset` function with a custom implementation that downloads the dataset files from the HF CDN.

```python
import requests

def download_dataset(repo_id, dataset_name):
    url = f"https://huggingface.co/datasets/{repo_id}/resolve/main/{dataset_name}"
    response = requests.get(url)
    with open(f"{dataset_name}.parquet", "wb") as f:
        f.write(response.content)

# Download the dataset
download_dataset("axentx/workio-dataset", "train.parquet")
```

#### Step 2: Pre-List File Paths and Embed in Training Script
Make a single API call to `list_repo_tree` to get the list of file paths for the dataset, and then embed this list in the `train.py` script. This will allow the script to download the dataset files from the HF CDN without making additional API calls.

```python
import json

# Load the list of file paths from the API call
with open("file_paths.json", "r") as f:
    file_paths = json.load(f)

# Embed the list of file paths in the training script
dataset_files = [f"https://huggingface.co/datasets/axentx/workio-dataset/resolve/main/{file_path}" for file_path in file_paths]
```

#### Step 3: Update `train.py` to Use CDN-Only Fetches
Modify the `train.py` script to use the embedded list of file paths to download the dataset files from the HF CDN. This will bypass the HF API rate limit and allow for faster dataset training.

```python
import requests

# Download the dataset files from the HF CDN
for file_url in dataset_files:
    response = requests.get(file_url)
    with open(file_url.split("/")[-1], "wb") as f:
        f.write(response.content)
```

By implementing these changes, we can improve the quality of the Workio project by reducing the reliance on the HF API and improving the speed of dataset training. This incremental improvement can be shipped in under 2 hours and will have a significant impact on the project's overall performance.
