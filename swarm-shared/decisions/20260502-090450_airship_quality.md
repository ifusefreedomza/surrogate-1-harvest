# airship / quality

# generated via cerebras:llama3.1-8b
### Summary
The provided text discusses various improvements and fixes for the Axentx product family, specifically focusing on the airship project located at `/opt/axentx/airship`. The focus is on quality improvements within the system, and the text outlines several patterns and fixes that have been identified and implemented.

### Patterns and Fixes
#### Business Research with Knowledge-RAG Pipeline
- **Fix**: After running a market analysis script (e.g., `granite-business-research.sh`), execute knowledge-rag to query the top hub and related docs for contextual insights.
- **Tags**: #business-research #knowledge-rag #graph

#### Top-Hub Doc Insight
- **Fix**: Review the most-connected hub (e.g., "MOC") before planning tasks.
- **Tags**: #knowledge-rag #graph #hub

#### Opus PR Reviewer Script Execution Error
- **Fix**: Ensure the wrapper script has a proper Bash shebang (`#!/usr/bin/env bash`), is executable (`chmod +x`), invokes via Bash (`bash <script> "$@"`), and sets `SHELL=/bin/bash` in the crontab.
- **Tags**: #bash #script-error #opus-pr-reviewer #cron

#### Active-Learning Wrapper Execution Error
- **Fix**: Ensure the wrapper script has a proper Bash shebang (`#!/usr/bin/env bash`), is executable (`chmod +x`), invokes via Bash (`bash <script> "$@"`), and sets `SHELL=/bin/bash` in the crontab.
- **Tags**: #script #cron #bash #active-learning #wrapper

### Surrogate-1 Training Pipeline
#### PyArrow CastError on HF Dataset with Mixed Schema Files
- **Fix**: Don't use `load_dataset(streaming=True)` for repos with heterogeneous file schemas. Download each file individually via `hf_hub_download` and project to `{prompt, response}` only at parse time.
- **Tags**: #training #pyarrow #hf-datasets #schema #surrogate-1

#### HF API Rate Limit 429 (1000 req/5min)
- **Fix**: Avoid `list_repo_files` recursion on big repos (paginates 100x). Use `list_repo_tree(path, recursive=False)` per folder. CDN downloads (`/resolve/main/`) are not counted. After 429: wait 360s before retry.
- **Tags**: #huggingface #rate-limit #api #ingestion

#### HF Commit Cap 128/hr/repo Blocks Ingestion
- **Fix**: Spread writes across N sibling repos (5 siblings = 640/hr aggregate). Hash slug → pick repo deterministically.
- **Tags**: #huggingface #commit-cap #throughput

#### Lightning H200 Not in Default Cloud Account
- **Fix**: Lightning has 9 cloud accounts; H200 only in `lightning-lambda-prod` (paid). Free tier falls to `lightning-public-prod` → L40S max. Sweep clouds × sizes in priority order.
- **Tags**: #lightning-ai #gpu #h200 #free-tier

#### Existing Lightning Studio Reuse
- **Fix**: Before `Studio(create_ok=True)`, list `Teamspace.studios` and reuse Running ones — saves 80hr/mo quota.
- **Tags**: #lightning-ai #quota

#### Dataset-Mirror Writes Mixed-Schema Files to Enriched/
- **Fix**: Project to `{prompt, response}` only before upload. Move attribution to
