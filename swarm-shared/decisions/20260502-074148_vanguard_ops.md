# vanguard / ops

### 1. **Diagnosis**
* The Vanguard project lacks a comprehensive solution to handle HF API rate limits, which can block dataset training and hinder the project's progress.
* The current implementation does not utilize the HF CDN bypass strategy, which can download public dataset files without being blocked by the HF API rate limit.
* The project's README file is missing, making it challenging for new developers to understand the project's purpose, context, and functionality.
* The absence of a README file leads to increased onboarding time and potential errors due to lack of documentation.
* The project's ops focus requires improvements in handling HF API rate limits, utilizing the HF CDN bypass strategy, and creating a comprehensive README file.

### 2. **Proposed change**
The proposed change involves creating a script to utilize the HF CDN bypass strategy and handle HF API rate limits. The script will be located in the `/opt/axentx/vanguard` directory and will be named `hf_cdn_bypass.py`. The script will download public dataset files using the HF CDN bypass strategy and handle HF API rate limits by waiting for a specified amount of time before retrying.

### 3. **Implementation**
The implementation involves creating the `hf_cdn_bypass.py` script and modifying the existing code to utilize the HF CDN bypass strategy. The script will use the `requests` library to download public dataset files from the HF CDN.

```python
import requests
import time

def download_dataset_file(url):
    try:
        response = requests.get(url)
        response.raise_for_status()
        return response.content
    except requests.exceptions.RequestException as e:
        print(f"Error downloading dataset file: {e}")
        return None

def handle_rate_limit(wait_time):
    print(f"Waiting for {wait_time} seconds due to HF API rate limit...")
    time.sleep(wait_time)

def main():
    dataset_url = "https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    wait_time = 360  # seconds

    # Download dataset file using HF CDN bypass strategy
    dataset_file = download_dataset_file(dataset_url)

    # Handle HF API rate limit
    if dataset_file is None:
        handle_rate_limit(wait_time)
        # Retry downloading dataset file
        dataset_file = download_dataset_file(dataset_url)

    # Save dataset file to local directory
    with open("dataset_file.parquet", "wb") as f:
        f.write(dataset_file)

if __name__ == "__main__":
    main()
```

### 4. **Verification**
To verify that the `hf_cdn_bypass.py` script works as expected, you can run the script and check that the dataset file is downloaded successfully. You can also check the script's output to ensure that it handles the HF API rate limit correctly.

```bash
python hf_cdn_bypass.py
```

Check that the dataset file is downloaded successfully:

```bash
ls dataset_file.parquet
```

Check the script's output to ensure that it handles the HF API rate limit correctly:

```bash
python hf_cdn_bypass.py | grep "Waiting for"
```
